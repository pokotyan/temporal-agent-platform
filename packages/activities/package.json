{
  "name": "@tap/activities",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@tap/shared": "*",
    "@temporalio/activity": "^1.11.0",
    "@kubernetes/client-node": "^1.0.0"
  }
}
